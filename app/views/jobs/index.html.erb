<h1>Listing jobs</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Organization</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @jobs.each do |job| %>
  <tr>
    <td><%= job.title %></td>
    <td><%= job.description %></td>
    <td><%= link_to 'Show', [job.organization, job] %></td>
    <td><%= link_to 'Edit', edit_organization_job_path(job.organization, job) %></td>
    <td><%= link_to 'Destroy', [job.organization, job], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Job', new_organization_job_path(params[:organization_id]) %>
